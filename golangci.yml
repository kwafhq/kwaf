run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - gosimple
    - unused
    - errcheck
    - gocyclo
    - ineffassign
    - gosec
issues:
  exclude-rules:
    - path: _test\.go
      linters: [gocyclo]
      text: "cyclomatic complexity"
